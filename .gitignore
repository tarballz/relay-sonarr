# Secrets & local config (never commit — use the .example templates)
.env
config.yaml

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# Frontend / build output
node_modules/
frontend/dist/
backend/app/static/

# Durable state (SQLite) — runtime data, never commit
data/
*.db
*.db-wal
*.db-shm
backend/relay.db*

# Editor / OS
.vscode/
.idea/
.DS_Store
*.log
